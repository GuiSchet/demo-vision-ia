# --- MinIO (usado por backend y frame-processor) ---
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET=cctv
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=secret123

# --- Qdrant (vector DB, usado por backend y frame-processor) ---
VECTOR_DB_URL=http://qdrant:6333

# --- LLM API (usado por backend y frame-processor) ---
QWEN_API_URL=https://api.qwen.ai/v1/vision
QWEN_API_KEY=tu_api_key

# --- Backend (FastAPI) ---
JWT_SECRET_KEY=tu_jwt_secret

# --- Frame-Processor ---
FRAME_INTERVAL_SEC=5

# --- Frontend (Next.js, solo las que empiezan con NEXT_PUBLIC_ serán públicas) ---
NEXT_PUBLIC_API_URL=http://api:8000